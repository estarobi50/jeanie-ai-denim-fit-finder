{
  "name": "jeanie",
  "version": "1.0.0",
  "private": true,
  "proxy": "http://localhost:3001",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.620.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "node server.js",
    "dev": "npm run server & npm start",
    "build": "react-scripts build",
    "deploy": "npm run build && cd infra && npm install && npm run deploy"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
